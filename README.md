# Odoo + PrestaShop Connector

## Arranque inicial
```bash
docker compose up -d
```
Esperar a que la instalacion completa de todos los coponentes termine

## Parar el entorno
```bash
docker compose stop
```
**NUNCA usar `docker compose down` ya que borra los datos.**

## Reiniciar
```bash
docker compose start
```